import 'package:flutter/material.dart';

class HashSeperatedItem extends StatelessWidget {
  const HashSeperatedItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 2,
      color: const Color(0xFFB7B7B7),
    );
  }
}
